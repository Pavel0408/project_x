import { sharedManagerModels } from './shared-manager-models';

describe('sharedManagerModels', () => {
  it('should work', () => {
    expect(sharedManagerModels()).toEqual('shared-manager-models');
  });
});
